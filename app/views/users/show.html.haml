.user
  = image_tag @user.avatar
  %h1
    = @user.firstname 
    = @user.lastname
  = render 'users/stats'
  = render 'follow_form' if user_signed_in?
  %h2
    = @user.location
  %p= @user.bio

  = render 'users/stats'
  = render 'follow_form' if user_signed_in?

#user-projects{data:{userid: params[:id]}}
